#ifndef GeneralClass
#define GeneralClass

#include <string>
#include <vector>

using namespace std;

void Split(const string& input, vector<string>& divided);
int Random(const int& min, const int& max);

#endif // !GeneralClass